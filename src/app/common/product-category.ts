export class ProductCategory { //class which refers to the product categories in the webshop

    //same thing like we could assign values and create getters/setters, this just cut a lot of code and save time
    constructor(public id: number, public categoryName: number) { }
}
